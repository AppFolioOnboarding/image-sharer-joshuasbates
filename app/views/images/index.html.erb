<div class = "album text-muted">
  <div class = "container">
    <div class="row">
      <% @images.each do |image| %>
        <div class="card">
          <%= image_tag image.imageurl, class: "mx-auto my-auto" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
