<%= stylesheet_link_tag "image" %>

<table class="image_table">
  <% @images.each do |image| %>
      <tr>
        <td><%= image_tag image.imageurl, class: "index_image" %></td>
      </tr>
  <% end %>
</table>
