<div class="js-image-tags-and-actions row">
  <span class="js-tags-list col-10 justify-content-start">
    <% image.tag_list.each do |tag| %>
      <%= link_to(tag, "#{images_path}?tag_name=#{tag}", :class=>"js-card-tag badge badge-default") %>
    <% end %>
  </span>
  <span class="js-card-btn-group btn-group col-2 justify-content-end">
    <%= button_to 'Delete', image_path(image),
                  method: :delete,
                  data: { confirm: 'Are you sure you want to delete this image?' },
                  class: "js-btn-del btn btn-secondary btn-sm" %>
  </span>
</div>
